@model ProjectYu.Models.LoginedUserModel

@{
    ViewData["Title"] = "Index";
}

<h2>Favorites</h2>

<div align="center">
    <table>

        @for (int i = 0; i < Model.UserModel.listOfFavorites.listOfMVideoModels.Count; i++)
        {
        <tr>
            <td>
                <video width="200" height="200" controls>
                    <source src='C:\\Users\\dparh\\Desktop\\@Model.UserModel.listOfFavorites.listOfMVideoModels.ElementAt(i).FileName' type="video/mp4">
                    Your browser does not support the video tag.
                </video>
            </td>
            <td>
                @Model.UserModel.listOfFavorites.listOfMVideoModels.ElementAt(i).VideoName
            </td>
        </tr>
                }
           
        </table>
</div>

